name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build core
        run: cd core && npm run build

      - name: Build server
        run: cd server && npm run build

      - name: Build dashboard
        run: cd dashboard && npm run build

      - name: Build gui
        run: cd gui && npm run build

      - name: Test core
        run: cd core && npm test

      - name: Test server
        run: cd server && npm test

      - name: Test dashboard
        run: cd dashboard && npm test

  release:
    needs: build-and-test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') }}

  # Uncomment when npm org and NPM_TOKEN secret are configured
  # publish-npm:
  #   needs: build-and-test
  #   runs-on: ubuntu-latest
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: npm
  #         registry-url: https://registry.npmjs.org
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Build all
  #       run: npm run build:all
  #
  #     - name: Publish @jacques/core
  #       run: cd core && npm publish --access public
  #       env:
  #         NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
  #
  #     - name: Publish @jacques/server
  #       run: cd server && npm publish --access public
  #       env:
  #         NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
  #
  #     - name: Publish jacques (CLI)
  #       run: cd dashboard && npm publish --access public
  #       env:
  #         NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
